# config-s03.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 0.20
  warn_above: 0.12
  asset_context:
    criticality: 1.0
    internet_facing: true
    requires_auth: true
  compensating_controls:
    - type: "waf"
      effectiveness: 0.60
      justification: "Strict WAF"

reporting:
  format: json
  output: report.json
  verbose: true
