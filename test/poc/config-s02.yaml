# config-s02.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 0.6  # normalizado v4 [0, 1.5]
  warn_above: 0.45  # normalizado v4
  asset_context:
    criticality: 0.95
    internet_facing: true
    requires_auth: false
  compensating_controls: []

reporting:
  format: json
  output: report.json
  verbose: true
