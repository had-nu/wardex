# config-s02.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 0.05
  warn_above: 0.03
  asset_context:
    criticality: 1.5
    internet_facing: true
    requires_auth: false
  compensating_controls: []

reporting:
  format: json
  output: report.json
  verbose: true
