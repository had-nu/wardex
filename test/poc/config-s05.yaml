# config-s05.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 0.6  # escala normalizada [0, 1.5] — v4
  warn_above: 0.25
  asset_context:
    criticality: 0.8
    internet_facing: true
    requires_auth: true

reporting:
  format: json
  output: report.json
  verbose: true
