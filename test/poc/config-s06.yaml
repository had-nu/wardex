# config-s06.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 4.0
  warn_above: 2.5
  asset_context:
    criticality: 0.95
    internet_facing: true
    requires_auth: false

reporting:
  format: json
  output: report.json
  verbose: true
