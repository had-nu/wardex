# config-s05.yaml
organization:
  name: "Wardex PoC"
release_gate:
  enabled: true
  risk_appetite: 0.50
  warn_above: 0.30
  asset_context:
    criticality: 1.0
    internet_facing: true
    requires_auth: true

reporting:
  format: json
  output: report.json
  verbose: true
