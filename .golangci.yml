version: "2"
run:
  timeout: 5m
  go: "1.24"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - gosec
    - misspell
    - revive
    - unconvert
    - unparam

linters-settings:
  revive:
    rules:
      - name: exported
        severity: warning
  gosec:
    excludes:
      - G204  # subprocess launched with variable — review each case

issues:
  exclude-rules:
    - path: "_test\\.go"
      linters:
        - gosec
